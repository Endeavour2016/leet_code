# 仓库协作与编码约定

> 创建日期：未知（历史文档未记录）
> 修改日期：2026-09-25
> AI辅助模型：GPT-6（未确认具体型号）

本文件适用于本仓库后续新增和修改的代码、测试及文档。修改应围绕用户当前任务，保留已有改动，不为统一风格重写无关的历史代码，不自动提交 Git。

## 目标与语言版本

- 本仓库用于面试准备和复习。先把问题、核心原理和实现框架讲清楚，再补充必要的边界条件。
- `engineering/` 面向有 C++ 和检索系统经验的读者，但不能假设读者已经理解某段代码或作者使用的简称。
- 工程题全部使用标准 C++11，关闭编译器扩展，不引入 C++14/17 的功能。历史算法目标沿用已有语言标准，不擅自修改。
- 优先使用标准库和简单、明确的接口。不要为了展示语法而加入复杂模板、过度抽象或第三方运行依赖。
- 教学实现要能编译、运行和验证，但不需要扩展成完整生产组件。明确支持哪些操作、适用于什么条件，以及尚未实现的能力。

## 代码格式与命名

- 代码风格以 Google C++ Style Guide 为基础；工程题以 `engineering/.clang-format` 为具体格式配置。
- 两空格缩进，控制语句使用完整花括号；不要把多个语句、多层条件或完整循环挤在一行。行宽目标为 80 列，避免为排版把中文注释拆得难读。
- 新标识符遵循 Google 命名习惯；修改已有接口时保持局部命名一致，不为了命名风格进行无关的大范围重命名。
- 直接包含用到的标准库头文件，不依赖其他头文件间接引入声明。头文件添加路径对应的防重复包含宏，并在结尾注明宏名。
- C++ 源码完成后执行 clang-format，并检查其改动；自动格式化不能代替代码审阅。

## 文件头与中文注释

每个 C++ 源文件和头文件都使用统一的 Doxygen 文件头。`@brief` 写用途，`@details` 写核心思路，`@note` 写调用限制；有对应题解时写 `@see`，可执行示例写运行命令。不要编造作者或版权信息。

```cpp
/**
 * @file 03_thread_pool.cpp
 * @brief 实现固定线程数的 C++11 线程池。
 * @details 持有互斥锁时取出任务，释放锁后执行，通过 future 获取结果。
 * @note 关闭后拒绝新任务，并执行完已接受的任务。
 * @see notes/engineering/03_thread_pool.md
 * @par 运行方式
 * 在仓库根目录执行：make run eng_thread_pool
 */
```

具体代码使用中文 `//` 注释，优先说明：

- 关键函数的前提、返回值含义、失败时输出参数是否改变。
- 数据属于哪个线程、由哪把锁保护、对象必须存活到什么时候。
- 为什么需要某个边界检查，或为什么必须按当前顺序操作。

简单赋值和直观循环不逐行翻译。注释应帮助理解原因，不要增加复习负担。

## 错误处理：不使用异常

- 新增和修改的 C++ 实现不使用 `throw`、`try/catch`、`set_exception` 等异常处理逻辑；工程目标通过编译选项禁用异常。
- 简单操作用 bool 表示成功或失败；调用方确实需要区分原因时，使用清晰的错误码枚举。任务既要返回状态又要返回数据时，使用简单结果结构体，不建立复杂错误处理框架。
- 写清返回值到底表示“操作成功”还是“查询结果”。例如 Bloom Filter 的查询失败不能被解释为“元素不存在”；线程池接受任务不等于任务业务执行成功。
- 可恢复失败时，默认保留原有状态和输出参数；先校验，必要时用临时结果计算，成功后再更新输出。确需部分完成的接口必须明确说明。
- 检查重要返回值。不能用空结果掩盖失败，也不能用 `abort()`、`CHECK` 或断言替代正常的参数错误、队列关闭等业务处理。
- `CHECK` 只用于测试；失败时报告位置并终止测试。不能把必要操作放进可能在 Release 中被删除的 `assert`。
- 禁用异常不代表标准库的所有失败都能通过错误码恢复。内存分配、操作系统线程创建等资源失败超出当前教学实现的恢复范围，文档必须如实说明；如任务要求恢复，应另行设计对应的资源接口。
- 不为禁用异常给所有函数机械添加 `noexcept`；它不能代替错误处理。`future.get()` 等接口必须满足有效状态、调用次数等使用条件。

## 并发与资源管理

- 使用 RAII 管理锁和资源；线程退出并完成 join 后，才能销毁它引用的对象。不要用 detach 回避生命周期问题。
- 条件变量等待必须检查共享状态；检查和修改该状态时持有同一把互斥锁。通知本身不是状态。
- 持有保护任务队列的锁时只处理队列和相关状态，执行任务前释放锁。不要持有工作线程退出所需的锁再 join。
- 说明关闭后是否继续执行已接受任务、哪些调用可并发、哪些必须由外部线程调用。
- 原子操作要解释数据可见性和内存序的依据；不要仅凭测试通过宣称没有数据竞争或保证无锁。

## 文档开头的元信息

- 后续新增或修改文档时，必须在文档开头（一级标题之后、正文之前）写明“创建日期、修改日期、AI辅助模型”，适用于 README、题解、学习笔记和其他说明文档。
- 日期统一使用 `YYYY-MM-DD`，按本地时区记录。新建文档时，创建日期与修改日期相同；后续修改保留创建日期，将修改日期更新为本次实际修改日期。
- “AI辅助模型”填写本次编写或修改文档实际使用的模型名称，例如 `GPT-6 Astra`；示例名称不能作为默认值。无法确认具体型号时如实注明“未确认具体型号”，不要猜测。
- 修改历史文档时，缺少元信息则补齐；创建日期只能依据可靠记录填写，无法确认时写“未知（历史文档未记录）”，不能把本次修改日期冒充创建日期。不为补齐元信息批量修改与当前任务无关的文档。

格式示例（日期和模型名称须替换为实际信息）：

```markdown
# 文档标题

> 创建日期：2026-09-25
> 修改日期：2026-09-25
> AI辅助模型：GPT-6 Astra

正文从这里开始。
```

## 题解与表达

- 先讲目标和直观例子，再讲核心步骤、正确性依据、复杂度和取舍。完整实现以可编译源码为准，文档只摘录关键片段并保持同步。
- 分开写“面试题目（提供给候选人）”和“面试官参考答案”。列明输入输出、限制、建议时长、分层追问及答案、评分与验证方式。
- 评分依据是可观察的分析和实现能力，区分独立完成、提示后完成及严重正确性错误，不按术语数量评分。
- 使用业界通用术语；专业词第一次出现时用通俗中文解释。不要自造词、堆砌缩写，或让读者根据上下文猜概念。
- 避免“题面”“核心不变量”“队列锁内”等未解释的简称。直接写“面试题目”“必须保证的条件”“持有保护队列的互斥锁时”。
- 解释复杂概念时给出小例子或反例，例如持锁执行慢任务会怎样阻塞其他线程，不只给一句结论。
- 不把具体公司的内部系统当作已知事实；引用专业机制时优先官方文档、源码或原始论文，区分教学模型与生产实现。

## 构建、测试与交付

- 工程题使用独立注册函数和 `eng_` 目标前缀，链接 `Threads::Threads`，接入现有 Make/CMake/CTest，单题测试设置超时。
- `make run eng_<题名>` 运行单题；`make test` 执行 Debug 测试及原有回归；`make release` 检查 Release 构建。改变执行逻辑时还应运行相关 Release 示例，不把编译成功当成运行验证。
- 数据结构与简单可靠的基线对照；随机测试固定种子。错误处理变更覆盖失败状态，以及失败后数据、输出参数是否保持约定。
- 并发测试用条件变量、promise 或其他同步协议协调，不用固定 sleep 推断正确性，不用跨机器性能阈值决定通过。
- ThreadSanitizer 可作为补充检查，但测试通过不替代生命周期、内存序和正确性分析。只报告实际执行的验证和结果。
- 格式或文档修改不新增重复实现的测试；根据改动范围选择验证。提交结果前检查源码、注释、题解中的接口和示例一致。
